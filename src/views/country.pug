include navbar 
doctype html 
html
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    style
      include ../styles/main.css
      include ../styles/cities.css
    title Pioneers | Home
  body.page  
    header
    section
      h2 Countries 
      table.table  
        thead 
            th Country Code 
            th Country Code 2
            th Name 
            th Continent 
            th Region
            th Surface Area 
            th Independence Year
            th Population 
            th Life Expectancy 
            th GNP 
            th GNPOld 
            th Local Name 
            th Goverment Form 
            th Head of State 
            th Capital
        tbody
          each country, id in countries  
            tr   
              td #{country.Code}
              td #{country.Code2}
              td #{country.Name}
              td #{country.Continent}
              td #{country.Region}
              td #{country.SurfaceArea}
              td #{country.IndepYear}
              - const population = new Intl.NumberFormat().format(country.Population);
              td.table-row.number #{population}
              td #{country.LifeExpectancy}
              td #{country.GNP}
              td #{country.GNPOld}
              td #{country.LocalName}
              td #{country.GovernmentForm}
              td #{country.HeadOfState}
              td #{country.Capital}
    footer
