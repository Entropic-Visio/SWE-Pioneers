include navbar 
doctype html 
html
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    style
      include ../styles/main.css
      include ../styles/cities.css
    title Pioneers | Home
  body.page  
    header
    section
      h2 Cities 
      table.table  
        thead 
            th City ID 
            th Name 
            th Country Code
            th District 
            th Population 
        tbody
          each city, id in cities  
            tr   
              td.table-row
                a(href=`/cities/id/${city.ID}`) #{city.ID}
              td.table-row #{city.Name}
              td.table-row #{city.CountryCode}
              td.table-row #{city.District}
              - const population = new Intl.NumberFormat().format(city.Population);
              td.table-row.number #{population}
    footer
